<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Γράψτε κάτι γρήγορα για να ανέβει..." %>
  </div>
  <%= f.submit "Δημοσίευση", class: "btn btn-large btn-primary" %>
<% end %>